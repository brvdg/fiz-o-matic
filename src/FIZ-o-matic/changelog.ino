/* 180906a
 * - many things... I forgot to write the changelog
 *
 * 180225a
 * - adding Serial UI
 *
 * 180219a
 * - published version
 *
 * 180214a
 * - add config storage on flash_config -> now works without SD
 *
 * 180102
 * - removed some little bugs
 * - add TINYGSM_DEBUG
 *
 * 171222a
 * - add a wtchdog for GPS
 * -
 *
 * 171204a
 * - nothing special
 * - add tests for ht16k33
 *
 * 171108a
 * - rename SIM808 to TinyGSM
 * - improve stay_online
 *
 * 171103a
 * - add support for LM75 on address 0x49 and 0x4A
 *
 * 171101a
 * - add status_checker as seperate sketch
 * - changed geo fence to permanent active
 * - changing menu logic to use pointer
 *
 * 171029a
 * - improved KML logging
 * -
 *
 * 171027a
 * - created pointer for configuration and variables
 * - improved serial output
 *
 * Version 0.8 Build 170928a
 * - adding functions to go online periodicly or permanent if ignition ist turned off
 * - adding more values for Blynk
 *
 * Version 0.8
 * Based on Hardware rev 0.8
 * - changing from Adafruit FONA library to TinyGSM
 * - adding a alarming system
 * - add an alarm if the light stills on after ignition turned off
 *
 * Version 170222a
 * - add OLED Display(SH1106 Chipset)
 * - renaming some Sketchs
 * - optimized Display functions
 *
 * Version 170125a
 * - add I2C sensor SI7021
 * - add VW Kombiinstrumen temperature sensor
 * - add function to dimm the backlight LED
 *
 * Version 170118a
 * - add "custom" functions for user defined code.
 */
